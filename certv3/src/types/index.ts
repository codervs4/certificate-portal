export type CertificateStatus='active'|'revoked'|'draft';
export interface Certificate { id:string; certificate_id:string; recipient_name:string; normalized_name:string; template_id:string; status:CertificateStatus; created_at:string; updated_at:string; download_count?:number; last_downloaded?:string|null; certificate_downloads?:Array<{count:number}>; }
export interface CertificateTemplate { id:string; template_name:string; template_file_url:string; template_width:number; template_height:number; name_x:number; name_y:number; name_width:number; name_height:number; font_family:string; font_file_url?:string|null; font_size:number; font_weight:number; text_alignment:'left'|'center'|'right'; text_color:string; letter_spacing:number; line_height:number; allow_multiline:boolean; min_font_size:number; created_at:string; updated_at:string; is_active?:boolean; }
export interface DownloadEvent { id:string; certificate_id:string; downloaded_at:string; download_status:string; user_agent:string|null; device_type:string|null; browser:string|null; approximate_location_if_available:string|null; session_identifier:string|null; }
export interface AdminActivity { id:string; admin_user_id:string; action:string; certificate_id:string|null; created_at:string; }
export interface TemplatePosition { x:number;y:number;width:number;height:number;fontFamily:string;fontSize:number;fontWeight:number;alignment:'left'|'center'|'right';color:string;letterSpacing:number;lineHeight:number;allowMultiline:boolean;minFontSize:number; }
export interface ExcelRow { name:string; rowNumber:number; }
export interface DbCertificateRow { id:string; certificate_id:string; recipient_name:string; normalized_name:string; template_id:string; status:CertificateStatus; created_at:string; updated_at:string; is_active?:boolean; }
