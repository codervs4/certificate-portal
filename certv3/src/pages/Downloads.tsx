import {useEffect,useState} from 'react';import {Card} from '../components/ui';import {recentDownloads} from '../services/analytics';import {formatDate} from '../lib/utils';
export default function Downloads(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{recentDownloads(100).then(setRows).catch(()=>{})},[]);return <div><div className="mb-6"><h1 className="text-2xl font-bold">Downloads</h1><p className="mt-1 text-sm text-slate-400">Recent certificate download activity.</p></div><Card className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-[#080b12] text-xs uppercase text-slate-400"><tr><th className="p-3">Certificate</th><th>Name</th><th>Downloaded</th><th>Device</th><th>Browser</th></tr></thead><tbody className="divide-y divide-white/10">{rows.map(r=><tr key={r.id}><td className="p-3 font-semibold">{r.certificates?.certificate_id}</td><td>{r.certificates?.recipient_name}</td><td>{formatDate(r.downloaded_at)}</td><td>{r.device_type??'—'}</td><td>{r.browser??'—'}</td></tr>)}</tbody></table>{!rows.length&&<div className="p-8 text-center text-sm text-slate-400">No download activity.</div>}</Card></div>}
