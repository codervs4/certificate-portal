import {Card} from '../components/ui';export default function Settings(){return <div><h1 className="text-2xl font-bold">Settings</h1><p className="mt-1 text-sm text-slate-400">Deployment and account configuration.</p><Card className="mt-5 p-5"><h2 className="font-semibold">Security</h2><p className="mt-2 text-sm leading-6 text-slate-400">Administrator access is controlled by Supabase Auth and the user's <code>app_metadata.role = admin</code>. Secrets are never stored in the frontend.</p></Card></div>}
